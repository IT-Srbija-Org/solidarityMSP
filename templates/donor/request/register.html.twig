{% extends 'base.html.twig' %}

{% block body %}
    {% import "macro.html.twig" as macro %}
    {{ macro.back('donor_request_donate') }}

    <div class="card card-lg bg-base-100 shadow m-4">
        <div class="card-body">
            <div class="text-center mb-14 mt-2">
                <div class="text-gray-500 uppercase text-sm">
                    Korak <span class="font-bold">2</span> od <span class="font-bold">3</span>
                </div>

                <div class="text-3xl font-bold text-gray-700">
                    Registruj se
                </div>
            </div>

            {{ include('flashes.html.twig') }}

            {% form_theme form 'form.html.twig' %}
            {{ form_start(form) }}
                <div class="space-y-4">
                    {{ form_errors(form) }}

                    <div>
                        {{ form_label(form.firstName) }}
                        {{ form_widget(form.firstName, { 'attr': { 'class': 'input input-md input-bordered rounded-md w-full' } }) }}
                        {{ form_errors(form.firstName) }}
                        <span class="text-gray-500 text-sm">Ime i prezime su nam potrebni zbog provere tvojih uplata ka oštećenim nastavnicima.</span>
                    </div>

                    <div>
                        {{ form_label(form.lastName) }}
                        {{ form_widget(form.lastName, { 'attr': { 'class': 'input input-md input-bordered rounded-md w-full' } }) }}
                        {{ form_errors(form.lastName) }}
                    </div>

                    <div>
                        {{ form_label(form.email) }}
                        {{ form_widget(form.email, { 'attr': { 'class': 'input input-md input-bordered rounded-md w-full' } }) }}
                        {{ form_errors(form.email) }}

                        {% if not app.user %}
                            <span class="text-gray-500 text-sm">Ako već imaš nalog na sajtu možeš se ulogovati <a href="{{ path('login') }}" class="link link-primary link-hover">ovde</a></span>
                        {% endif %}
                    </div>

                    <div>
                        {{ form_label(form.haveSerbianAccount) }}
                        {{ form_widget(form.haveSerbianAccount, { 'attr': { 'class': 'checkbox' } }) }}
                        {{ form_errors(form.haveSerbianAccount) }}

                        <div class="text-gray-500 text-sm -mt-2">Trenutno je preko naše platforme moguće donirati samo ako imaš dinarski račun, ako želiš da pomogneš nastavnicima iz inostranstva to možeš da uradite preko <a href="https://fondacijaalekkavcic.org/doniraj/" target="_blank" class="link link-primary link-hover">Fondacije Alek Kavčić</a>.</div>
                    </div>

                    {{ include('captcha.html.twig') }}
                    {{ form_row(form.submit, { 'attr': { 'class': 'btn btn-primary w-full' } }) }}
                </div>
            {{ form_end(form) }}
        </div>
    </div>
{% endblock %}
